export const Mongo = {
  Collection: class Collection {
    constructor() {}
    attachSchema = (schema) => {}
    findOneAsync = () => {
      return new Promise((resolve, reject) => {
        resolve('')
      })
    }
  },
}
